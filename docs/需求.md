# 个人门户系统详细需求文档 (Product Requirements Document)

本方案围绕 **基础认证、游戏轨迹、知识智库、资源宝库** 四大核心板块，详细定义了各端（前台、后台、移动端、API）的功能与交互逻辑。

---

## 0. 基础认证与单点登录 (Auth & SSO)

### 0.1 核心认证页面 (前台/后台通用)
*   **登录页 (Login)**
    *   **样式**: 简约居中卡片，背景采用动态粒子或毛玻璃效果。
    *   **交互**: 支持账号密码登录、记住我（Persistent Session）、第三方登录（GitHub/WeChat 预留）。
*   **注册页 (Register)**
    *   **样式**: 分步表单或简洁列表。
    *   **交互**: 包含用户名重复校验、密码强度提示。
*   **忘记密码 (Forgot Password)**
    *   **交互**: 邮箱验证码校验 -> 重置新密码。

### 0.2 [单点登录 (SSO)](./知识点.md#sso) 需求
*   **目标**: 用户在 `blog-web` (前台) 登录后，访问 `blog-admin` (后台) 时无需再次输入密码，实现一端登录，全端通用。
*   **实现逻辑**:
    1.  **统一认证中心**: `blog-api` 作为唯一的 Identity Provider。
    2.  **Cookie 共享**: 在同一根域名下（如 `*.me.com`），利用 Domain Cookie 共享 JWT Token。
    3.  **Token 校验**: 各端启动时检查本地存储/Cookie 中的 Token，并向后端发起 `/auth/me` 校验，实现无感登录。

---

## 1. 板块一：游戏轨迹 (Game Hub)

### 1.1 前台 Web (展示端)
*   **页面：游戏时光墙 (Game Wall)**
    *   **样式**: 采用大卡片流（Card Grid），背景高斯模糊（取自当前游戏封面颜色）。
    *   **交互**: 鼠标悬停显示最后游玩时间及奖杯进度条（PS风格）。
*   **页面：游戏详情页**
    *   **样式**: 宽屏布局，左侧封面，右侧统计图表（总时长、近30天活跃度）。
    *   **交互**: 点击成就图标弹出成就达成详情；点击“前往Steam/PSN”外链。
*   **页面：全平台愿望单**
    *   **样式**: 列表模式，醒目显示当前价格及跌幅百分比。

### 1.2 后台 Admin (管理端)
*   **页面：同步中心**
    *   **功能**: 配置 Steam API Key、PSN ID；手动触发数据同步。
    *   **样式**: 仪表盘展示上次同步时间、同步成功/失败条数。
*   **页面：游戏库维护**
    *   **功能**: 修正自动抓取失败的封面图或描述信息。

### 1.3 移动端 App (移动查阅)
*   **页面：游玩简报**
    *   **样式**: 简约列表，侧重展示“最近在玩”及“愿望单价格变动”。

---

## 2. 板块二：知识智库 (CMS)

### 2.1 前台 Web (阅读端)
*   **页面：文档概览 (Tree View)**
    *   **样式**: 左侧侧边栏展示树状目录，右侧主体展示文章内容。
    *   **交互**: 自动生成右侧悬浮文章大纲（TOC）；支持代码块一键复制。
*   **页面：全文搜索**
    *   **交互**: 搜索框输入时实时模糊匹配标题及内容片段。

### 2.2 后台 Admin (创作端)
*   **页面：文档编辑器 (Markdown Editor)**
    *   **样式**: 左右分栏预览；支持图片粘贴自动上传至 OSS。
    *   **功能**: 自动保存草稿；保存时可填写“版本说明”。
*   **页面：版本控制中心**
    *   **样式**: 时间轴展示版本历史；支持两版本代码对比（Diff View）。

### 2.3 移动端 App (碎片化查阅)
*   **页面：阅读器**
    *   **样式**: 适配手机屏幕的排版；支持长按选取文字生成分享卡片。

---

## 3. 板块三：资源宝库 (Resource Hub)

### 3.1 前台 Web (收藏展示)
*   **页面：资源瀑布流 (Resource Masonry)**
    *   **样式**: Pinterest 风格瀑布流，展示网站缩略图、标题、描述。
    *   **交互**: 点击分类标签（如：AI工具、设计素材）快速过滤。

### 3.2 后台 Admin (采集端)
*   **页面：智能剪藏助手**
    *   **交互**: 输入 URL 后，系统自动调用爬虫抓取 `og:title`, `og:description`, `favicon`；用户仅需确认分类并保存。
*   **页面：分类/标签管理**
    *   **功能**: 支持拖拽调整分类排序。

### 3.3 移动端 App (快捷录入)
*   **页面：快捷收藏**
    *   **交互**: 检测剪贴板 URL 自动弹出提示框确认收藏；支持系统的“分享”菜单直接导入。

---

<span id="api"></span>

## 4. 后端 API 接口定义 (NestJS - `blog-api`)

本节详细说明 API 模块划分及核心接口逻辑，前台与后台共用一套 Base URL。

### 4.1 通用基础模块 (Auth & Common)
*   `POST /auth/login`: 登录并返回 JWT Token。
*   `POST /common/upload`: 通用文件/图片上传接口（接入 OSS）。

### 4.2 游戏模块 (Game Module)
*   `GET /games/list`: 获取已同步的游戏记录（支持分平台、排序筛选）。
*   `GET /games/:id`: 获取单个游戏详细统计。
*   `POST /games/sync/trigger`: 触发爬虫/API 同步任务。
*   `GET /games/wishlist`: 获取跨平台愿望单汇总。

### 4.3 文档模块 (Doc Module)
*   `GET /docs/tree`: 获取文档目录树结构。
*   `GET /docs/:id`: 获取文档详情。
*   `POST /docs/upsert`: 发布/更新文档（支持版本备注）。
*   `GET /docs/:id/versions`: 获取文档所有历史版本。
*   `POST /docs/version/rollback`: 将文档回滚至指定版本。

### 4.4 资源模块 (Resource Module)
*   `POST /resources/scrape`: 传入 URL，后端返回抓取的元数据（用于前端预览）。
*   `POST /resources/add`: 最终保存资源链接。
*   `GET /resources/list`: 分页获取资源列表。
*   `GET /resources/categories`: 获取资源分类列表。

---
**由 Gemini CLI 需求分析更新 @ 2026-04-02**
