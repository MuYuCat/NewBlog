# 技术核心知识点解析 (Core Knowledge Points)

本文件用于深入解析博客系统中涉及的关键技术概念、架构选择及其核心逻辑，方便开发者快速理解底层原理。

---

<span id="monorepo"></span>

## 1. Monorepo (单仓库多项目)

### 1.1 什么是 Monorepo？
**Monorepo** (Monolithic Repository) 是一种项目代码管理策略，指将**多个项目**（如前端 Vue、后台 React、后端 NestJS、共享 UI 组件库、通用工具函数等）存储在**同一个 Git 代码仓库**中。

它与传统的 **Multi-repo**（每个子项目拥有独立仓库）相对，是目前互联网大厂（如 Google, Meta, 字节跳动）主流的代码管理方式。

### 1.2 为什么在本项目中使用 Monorepo？
针对本博客系统（包含 Web、Admin、App、API 四个端），Monorepo 带来了以下核心优势：

1.  **共享类型定义 (Shared Types)**:
    *   这是最强大的功能。可以在 `packages/common` 中定义一套 TypeScript 接口。
    *   **示例**: 后端 NestJS 定义了文章的数据结构，前端 Vue 和 React 可以直接引用该文件。字段修改时，前后端会同步报错，彻底杜绝接口不一致的问题。
2.  **依赖管理统一**:
    *   所有项目共享一套代码规范配置（ESLint, Prettier, TypeScript Config）。
    *   统一依赖版本，避免不同项目间因库版本冲突导致的“环境地狱”。
3.  **原子提交 (Atomic Commits)**:
    *   一次 Git 提交可以同时包含后端接口改动及其对应的所有前端调用逻辑。
    *   功能迭代时，代码库始终处于同步状态。
4.  **本地引用极简**:
    *   无需将公共包发布到 npm，通过 pnpm 的 `workspace` 机制即可像引用本地文件一样互相关联。

### 1.3 核心工具推荐
*   **[pnpm workspaces](https://pnpm.io/zh/workspaces)** (基础设施): 处理 package 之间的依赖链接。
*   **[Turborepo](https://turbo.build/)** (构建加速): 提供增量构建与缓存机制，大幅缩短 CI/CD 时间。
*   **[Nx](https://nx.dev/)** (全栈框架): 提供依赖图可视化、代码生成器等高级功能。

### 1.4 学习资源
*   [Monorepo.tools](https://monorepo.tools/): 全面、客观的 Monorepo 科普与工具对比站。
*   [精读《Monorepo 的优势》](https://github.com/dt-fe/weekly/blob/master/111.%E7%B2%BE%E8%AF%BB%E3%80%8AMonorepo%20%E7%9A%84%E4%BC%98%E5%8A%BF%E3%80%8B.md): 深度分析 Monorepo 适用场景的文章。

---

<span id="sso"></span>

## 2. 单点登录 (Single Sign-On / SSO)

### 2.1 什么是 SSO？
**单点登录** (Single Sign-On, 简称 SSO) 是一种让用户**只需登录一次**，就可以访问所有相互信任的子系统的机制。在本项目中，意味着用户在“前台”登录后，直接打开“后台管理”时无需重复输入账号密码。

### 2.2 本项目的实现方案 (Domain Cookie + JWT)
针对 [Monorepo](./知识点.md#monorepo) 的共享域名场景，本项目选择了最轻量、最稳定的 **基于 Cookie 共享的 SSO** 方案：

1.  **域名策略**: 将所有应用部署在同一个顶级域名的子域名下（如 `web.me.com` 和 `admin.me.com`）。
2.  **JWT 签发**: 后端 `blog-api` 签发 JWT Token。
3.  **Cookie 共享**: 
    *   在登录成功后，后端将 Token 写入 Cookie。
    *   关键配置：设置 `Domain: .me.com`。这使得浏览器在请求任何以 `.me.com` 结尾的域名时，都会自动携带这个 Cookie。
4.  **自动鉴权**: 
    *   子应用启动时，从 Cookie 中读取 Token。
    *   通过 API 校验 Token 合法性。
    *   如果合法，直接跳转至首页；不合法则重定向至统一登录页。

### 2.3 为什么不用 OAuth2 / CAS？
*   **复杂度**: 对于个人或中小型企业项目，OAuth2 (如 Keycloak) 过于沉重，增加了运维成本。
*   **适用性**: 我们的子应用都在同一个组织内（同一根域名），直接使用 Domain Cookie 是性能最高、体验最无感的。

### 2.4 未来扩展: 第三方 SSO
后期若要接入 GitHub、Google 或企业级 LDAP 登录，只需在后端 `blog-api` 中新增对应的 `Passport Strategy` 即可，前端逻辑基本保持不变。

---
**由 Gemini CLI 知识点更新 @ 2026-04-02**
